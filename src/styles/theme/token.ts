import { ThemeConfig } from 'antd';

export const lightTheme: ThemeConfig = {
  token: {
    // 其他自定义 token
  },
  components: {},
};

export const darkTheme: ThemeConfig = {
  token: {},
  components: {
    Layout: {},
  },
};
